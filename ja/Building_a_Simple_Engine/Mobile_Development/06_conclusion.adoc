:pp: {plus}{plus}

= モバイル開発: 結論

== 結論

=== すべてをまとめる

これらすべてのコンポーネントが、完全なモバイル向けに最適化された Vulkan アプリケーションでどのように連携できるかを見てみましょう。

[source,cpp]
----
class MobileOptimizedEngine {
public:
    MobileOptimizedEngine() {
        // Initialize platform-specific components
        #ifdef __ANDROID__
            initialize_android();
        #elif defined(__APPLE__)
            initialize_ios();
        #else
            initialize_desktop();
        #endif

        // Initialize Vulkan with mobile optimizations
        initialize_vulkan();
    }

    void run() {
        // Main application loop
        while (!should_close()) {
            handle_platform_events();
            update();
            render();
        }

        cleanup();
    }

private:
    void initialize_vulkan() {
        // Create instance
        vk::InstanceCreateInfo instance_info;
        // ... set instance parameters
        instance = vk::createInstance(instance_info);

        // Select physical device
        physical_device = select_physical_device(instance);

        // Detect if we're on a TBR GPU
        is_tbr_gpu = is_likely_tbr_gpu(physical_device);

        // Check for extension support
        auto available_extensions = physical_device.enumerateDeviceExtensionProperties();
        std::vector<const char*> supported_extensions = { VK_KHR_SWAPCHAIN_EXTENSION_NAME };

        // Add mobile-specific extensions if supported
        if (check_extension_support(available_extensions, VK_KHR_DYNAMIC_RENDERING_EXTENSION_NAME)) {
            supported_extensions.push_back(VK_KHR_DYNAMIC_RENDERING_EXTENSION_NAME);
            use_dynamic_rendering = true;
        }

        if (check_extension_support(available_extensions, VK_KHR_DYNAMIC_RENDERING_LOCAL_READ_EXTENSION_NAME)) {
            supported_extensions.push_back(VK_KHR_DYNAMIC_RENDERING_LOCAL_READ_EXTENSION_NAME);
            use_dynamic_rendering_local_read = true;
        }

        if (check_extension_support(available_extensions, VK_EXT_SHADER_TILE_IMAGE_EXTENSION_NAME)) {
            supported_extensions.push_back(VK_EXT_SHADER_TILE_IMAGE_EXTENSION_NAME);
            use_shader_tile_image = true;
        }

        // Create logical device with supported extensions
        vk::DeviceCreateInfo device_info;
        device_info.setPEnabledExtensionNames(supported_extensions);
        // ... set other device parameters
        device = physical_device.createDevice(device_info);

        // Initialize other Vulkan resources
        // ...
    }

    void render() {
        // Begin frame
        auto cmd_buffer = begin_frame();

        if (use_dynamic_rendering) {
            // Use dynamic rendering
            vk::RenderingAttachmentInfoKHR color_attachment;
            // ... set attachment parameters

            vk::RenderingInfoKHR rendering_info;
            // ... set rendering parameters

            cmd_buffer.beginRenderingKHR(rendering_info);

            // Record drawing commands
            // ...

            cmd_buffer.endRenderingKHR();
        } else {
            // Use traditional render passes
            // ...
        }

        // End frame
        end_frame(cmd_buffer);
    }

    // Platform-specific initialization
    void initialize_android() {
        // Android-specific setup
        // ...
    }

    void initialize_ios() {
        // iOS-specific setup with MoltenVK
        // ...
    }

    void initialize_desktop() {
        // Desktop-specific setup
        // ...
    }

    // Helper functions
    bool check_extension_support(const std::vector<vk::ExtensionProperties>& available, const char* extension_name) {
        for (const auto& ext : available) {
            if (strcmp(extension_name, ext.extensionName) == 0) {
                return true;
            }
        }
        return false;
    }

    bool is_likely_tbr_gpu(vk::PhysicalDevice device) {
        vk::PhysicalDeviceProperties props = device.getProperties();

        // Most mobile GPUs from these vendors use TBR
        if (props.vendorID == 0x5143 ||  // Qualcomm
            props.vendorID == 0x1010 ||  // PowerVR
            props.vendorID == 0x13B5 ||  // ARM Mali
            props.vendorID == 0x19E5 ||  // Huawei
            props.vendorID == 0x106B) {  // Apple
            return true;
        }

        return false;
    }

    // Vulkan objects
    vk::Instance instance;
    vk::PhysicalDevice physical_device;
    vk::Device device;

    // Flags
    bool is_tbr_gpu = false;
    bool use_dynamic_rendering = false;
    bool use_dynamic_rendering_local_read = false;
    bool use_shader_tile_image = false;
};
----

=== 出荷準備チェックリスト

1. 機能の検出とフォールバック: 起動時に EXT/KHR サポートを調査し、条件付きで有効にし、テストされたフォールバック パスを維持します。
2. レンダリング パスの選択: 単純なベンダー/ヒューリスティック チェックに基づいて、実行時に TBR フレンドリーなパスと IMR ニュートラルなパスを切り替えます。
3. フレームバッファ読み取りポリシー: タイルローカルのピクセル単位の読み取りを優先します (入力添付ファイルまたは動的レンダリングのローカル読み取り)。外部メモリの往復を強制するパターンは避けてください。
4. テクスチャとアセット: KTX2 をコンテナとして使用します。必要に応じて ETC2/PVRTC フォールバックが利用可能な場合は ASTC を優先します。ミップマップをオフラインで生成します。
5. メモリ/添付ファイル: パス後に結果が必要ない場合は、一時的な添付ファイルを使用します。断片化を最小限に抑えるためにサブ割り当てを行います。
6. サーマル/パフォーマンス ガバナ: 動的解像度または品質層と賢明な FPS キャップを実装して、サーマルを抑制します。
7. インストルメンテーション: GPU マーカー/タイムスタンプ、フレーム時間ヒストグラム、帯域幅プロキシを追加して回帰を追跡します。
8. デバイス マトリックス: 小規模で代表的なデバイス ラボ (さまざまなベンダー/層) を維持し、健全性シーンを定期的に実行します。

=== 検証とプロファイリングのハンドブック

- 正確さを検証します:
  * デバイスごとのスワップチェーンの詳細 (現在のモード、最小イメージ数)。
  * レイアウト遷移とアクセス マスク (特にローカル読み取りを使用する場合)。
  * レンダリング スコープと計算/転送作業間の同期。
- 効率的にプロファイリングを行う:
  * プラットフォーム ツール (Android GPU Inspector、RenderDoc、Xcode GPU Capture など) を使用して、タイル フラッシュ、オーバードロー、帯域幅のホット スポットを特定します。
  * A/B テスト: クラシック レンダー パスとダイナミック レンダリング、ローカル読み取りのオン/オフ、タイル イメージのオン/オフ。
  * 単一フレームだけでなく、数分間の実行にわたって電力と熱を追跡します。

=== 次のステップ

- 機能ビット (動的レンダリング、ローカル読み取り、タイル イメージ) を上位システムに公開する機能レイヤーを統合します。
- フィールド テレメトリのログにデバイス/機能情報をダンプする自動起動プローブを追加します。
- 回帰シーン スイートを拡張して、TBR に敏感なパスや帯域幅が重いパスをカバーします。

==== 高度なトピックを探索する (簡単なエンジンのチュートリアル)

次の短く焦点を絞ったチュートリアルは、Simple Engine 上に直接構築されており、次のステップとして最適です。

- xref:Building_a_Simple_Engine/Advanced_Topics/01_introduction.adoc[チュートリアル インデックス — すべてのトピックを参照する]
- xref:Building_a_Simple_Engine/Advanced_Topics/Mipmaps_and_LOD.adoc[ミップマップと LOD] — 安定したテクスチャ サンプリングと異方性に関する実践的なガイダンス。
- xref:Building_a_Simple_Engine/Advanced_Topics/Dynamic_Rendering_Local_Read.adoc[動的レンダリングのローカル読み取り] — サポートされている場合、タイル/ローカル メモリを介した同一パス読み取りを最適化します。

=== コード例

この章の完全なコードは、次のファイルにあります。

link:{attachmentsdir}/simple_engine/36_mobile_platform_integration.cpp[モバイル プラットフォーム統合 C{pp} コード]
link:{attachmentsdir}/simple_engine/37_mobile_optimizations.cpp[モバイル最適化 C{pp} コード]
link:{attachmentsdir}/simple_engine/38_tbr_optimizations.cpp[TBR 最適化 C{pp} コード]
link:{attachmentsdir}/simple_engine/39_mobile_extensions.cpp[モバイル拡張機能 C{pp} コード]

xref:Building_a_Simple_Engine/Mobile_Development/05_vulkan_extensions.adoc[前: モバイル用の Vulkan 拡張機能] | xref:../index.adoc[シンプルなエンジンの構築に戻る]

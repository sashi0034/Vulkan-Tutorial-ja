:pp: {plus}{plus}
= エンジンへの組み込み

この章では、`renderer_rendering.cpp` の既存ダイナミックレンダリングパスを活用し、直接的な空間レンダリングへ移行しました。レガシー状態を迂回し、眼固有のビューポート管理を可能にします。実装例は link:../../../attachments/openxr_engine/renderer_rendering.cpp[renderer_rendering.cpp] と link:../../../attachments/openxr_engine/renderer_pipelines.cpp[renderer_pipelines.cpp] にあります。

== 空間スワップチェーンへの直接描画

`renderer_rendering.cpp` では、`Render` 関数がすでに `vk::RenderingInfo` を使っています。OpenXR をサポートするには、正しいランタイムイメージを指すよう保証しなければなりません。

[source,cpp]
----
// renderer_rendering.cpp
void Renderer::Render(...) {
    // ... Wait for runtime to be ready ...
    uint32_t imageIndex = xrContext.acquireSwapchainImage();
    xrContext.waitSwapchainImage();

    for (uint32_t eye = 0; eye < 2; ++eye) {
        // 1. Point rendering at the acquired XR image view
        // Our engine uses a single multiview swapchain at index [0]
        vk::RenderingAttachmentInfo colorAttachment{
            .imageView = *eyeSwapchainImageViews[0][imageIndex],
            .imageLayout = vk::ImageLayout::eColorAttachmentOptimal,
            .loadOp = vk::AttachmentLoadOp::eClear,
            .storeOp = vk::AttachmentStoreOp::eStore,
            .clearValue = vk::ClearValue{vk::ClearColorValue{std::array<float, 4>{0.1f, 0.1f, 0.1f, 1.0f}}}
        };

        vk::RenderingInfo passInfo{
            .renderArea = vk::Rect2D({0, 0}, swapChainExtent),
            .layerCount = 1,
            .viewMask = (1u << eye), // Render specifically to this eye's layer
            .colorAttachmentCount = 1,
            .pColorAttachments = &colorAttachment
        };

        commandBuffer.beginRendering(passInfo);

        // Issue draws for this eye
        RenderEye(commandBuffer, eye);

        commandBuffer.endRendering();
    }

    // 2. Hand the image back to OpenXR
    xrContext.releaseSwapchainImage();
}
----

== ステレオビューポートの管理

ヘッドセットでは、2 つの異なる視点から描画しなければなりません。エンジンの `Render` ループでは、ビュー固有のビューポートとシザーを適用します。

[source,cpp]
----
// renderer_rendering.cpp
void Renderer::Render(...) {
    // ... inside the render loop ...

    for (uint32_t eye = 0; eye < 2; ++eye) {
        // Retrieve asymmetric projection and eye-specific viewport from OpenXR
        // xrContext provides these based on xrLocateViews
        auto viewport = xrContext.getViewport(eye);
        auto scissor = xrContext.getScissor(eye);

        commandBuffer.setViewport(0, viewport);
        commandBuffer.setScissor(0, scissor);

        // Update the eye-specific View and Projection matrices in our UBO
        // Note: Shaders don't need to change yet! We are rendering each eye sequentially.
        updateUniformBuffer(currentFrame, eye);

        // Draw the scene for this eye
        // ...
    }
}
----

== updateUniformBuffer ヘルパー

エンジンは現在、眼を **順次**（次々に）描画しているため、既存シェーダーを再利用できます。各眼パスで `UniformBufferObject` のビューと投影行列を更新するだけで済みます。

[source,cpp]
----
// renderer_rendering.cpp
void Renderer::updateUniformBuffer(uint32_t currentFrame, uint32_t eye, const XrPosef* overridePose) {
    // 1. Retrieve the predicted View and Projection from OpenXR
    // If overridePose is null, we query the latest from the context
    glm::mat4 viewMatrix = overridePose ? xrPoseToMatrix(*overridePose) : xrContext.getViewMatrix(eye);
    glm::mat4 projMatrix = xrContext.getProjectionMatrix(eye);

    // 2. Update the UBO for the current frame and eye
    UniformBufferObject ubo{};
    ubo.view = viewMatrix;
    ubo.proj = projMatrix;
    ubo.camPos = overridePose ? glm::vec3(overridePose->position.x, overridePose->position.y, overridePose->position.z)
                              : xrContext.getEyePosition(eye);

    // 3. Directly update the mapped memory
    // uniformBuffersMapped is indexed by frame and eye
    memcpy(uniformBuffersMapped[currentFrame][eye], &ubo, sizeof(UniformBufferObject));
}
----

== なぜこれらの変更か？

XR イメージを `vk::RenderingInfo` に直接バインドし、眼ごとにビューポート／シザーを更新することで、中間の「Blit」パスが不要になります。すべての GPU サイクルがメモリコピーではなく空間コンテンツに使われることが保証されます。この段階では、本質的に異なる視点から「世界を二度描いている」だけなので、シェーダーは変更不要です。後の章では、**マルチビュー** がこれら 2 パスを 1 つにまとめられることを見ます。

xref:OpenXR_Vulkan_Spatial_Computing/04_Dynamic_Rendering/03_stereo_viewport_scissor.adoc[前へ] | xref:OpenXR_Vulkan_Spatial_Computing/05_Predictive_Frame_Loop/01_introduction.adoc[次へ]
